You are a senior ReactJS engineer with strong experience in React Navigation, state management, mobile UX, and production-grade frontend architecture.

Objective
Complete all remaining frontend tasks listed below. Follow the existing project architecture, coding standards, UI design system, and navigation patterns. Reuse existing components whenever possible and avoid introducing duplicate implementations.

1. Language Settings
Implement the Language Settings feature.

Requirements:

Display available languages supported by the application.
Allow users to switch languages.
Persist the selected language across app restarts.
Ensure language changes are applied immediately without requiring the user to log out.
Integrate with the existing i18n/localization framework if one already exists.
2. Currency Settings
Implement the Currency Settings feature.

Requirements:

Display available currencies.
Allow users to select their preferred currency.
Persist the selected currency.
Ensure all monetary values throughout the application use the selected currency format.
Follow existing localization and formatting standards.
3. FAQ Screen
Rebuild the FAQ screen in React.

Requirements:

Locate the existing FAQ page that was implemented outside of ReactJS.
Extract and reuse all FAQ content from the existing implementation.
Existing Laravel Pages Must Remain Intact: The project currently contains existing FAQ and Data & Privacy Policy pages implemented in the Laravel web application (non-React implementation). Under no circumstances should these existing Laravel pages be removed, renamed, modified, replaced, or broken.
Preserve all content.
Improve responsiveness and maintainability where appropriate.
Follow the existing design system.
4. Data & Privacy Policy Screen
Rebuild the Data & Privacy Policy screen in React.

Requirements:

Locate the existing non-React implementation.
Extract and reuse all existing content.
Follow the application's UI standards.
5. Light Mode Support
Implement Light Mode.

Requirements:

Review the current theme architecture.
Add complete Light Mode support.
Ensure all screens, dialogs, menus, forms, cards, and navigation components support both Dark Mode and Light Mode.
Avoid hardcoded colors.
Use centralized theme tokens and design variables.
Persist user theme preferences.