You are an Expert Frontend Engineer specializing in React, TypeScript, and Internationalization (i18n). We have audited our UI pages and found several screens with hardcoded text strings failing to translate.

### 1. CONTEXT & REFERENCE ARCHITECTURE
- We have completely migrated to a new localization framework. 
- You MUST strictly follow the implementation blueprint, hook patterns, and architectural rules defined in: `docs/issues/i18n-setting-poc-2026-06-26.md`.
- **CRITICAL RULE:** Do NOT use the legacy context `TranslationContext` or `translations.json`. Use the standard `react-i18next` package library.

### 2. TARGET FILES FOR IMPLEMENTATION
Refactor the following 4 pages to fully support dynamic localization:
1. `resources/js/template/pages/Notification.tsx`
2. `resources/js/template/pages/NotificationSetting.tsx`
3. `resources/js/template/pages/PersonalInfo.tsx`
4. `resources/js/template/pages/MarketingScreen.tsx`

### 3. IMPLEMENTATION & CODE QUALITY REQUIREMENTS
- **Hook Extraction:** Correctly import and initialize the translation hook at the top of each component:
  `import { useTranslation } from 'react-i18next';`
  `const { t } = useTranslation();`
- **String Extraction:** Extract every single piece of user-facing text (headings, labels, placeholders, button titles, toast alerts, helper texts) and replace them with standard translation keys (e.g., `t('notification.title')`).
- **Layout & Design DNA Resilience:** Ensure that the UI bounding boxes, layout flex/grid properties, and component spacings are resilient to text expansion (since localized Vietnamese/English text strings vary greatly in length). Do not introduce text wrapping bugs or clipping.
- **TypeScript Strictness:** Maintain flawless type-safety. Do not bypass or introduce any `any` types during string extractions or mapping.

### 4. DELIVERABLES
Provide a clean, step-by-step implementation summary:
1. The exact refactored code blocks for each of the 4 targeted pages.
2. A list of the new translation keys and default fallback strings created for these pages so they can be merged into our locale JSON resource bundles.
3. Confirmation that the application builds smoothly without any Vite or TypeScript compilation errors.